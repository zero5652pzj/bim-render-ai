# 文字内容变更清单

本文档详细列出所有需要变更的文字内容。

## 一、登录页面 (LoginView.vue)

### 1. 表单标签文字

| 位置 | 当前文字 | 目标文字 | 说明 |
|------|----------|----------|------|
| label标签 | `手机号` | `邮箱` | 第175行 |

### 2. 输入框占位符

| 位置 | 当前文字 | 目标文字 | 说明 |
|------|----------|----------|------|
| placeholder | `请输入手机号` | `请输入邮箱地址` | 第187行 |

### 3. 验证错误提示

| 场景 | 当前文字 | 目标文字 | 说明 |
|------|----------|----------|------|
| 空字段验证 | `请输入手机号` | `请输入邮箱地址` | 第26行 |
| 格式错误验证 | `请输入有效的手机号（中国大陆）` | `请输入有效的邮箱地址` | 第31行 |

### 4. 登录失败提示

| 场景 | 当前文字 | 目标文字 | 说明 |
|------|----------|----------|------|
| 登录失败 | `登录失败，请检查手机号和密码` | `登录失败，请检查邮箱和密码` | 第100行 |

### 5. HTML属性

| 属性 | 当前值 | 目标值 | 说明 |
|------|--------|--------|------|
| id | `phone` | `email` | 第182行 |
| autocomplete | `tel` | `email` | 第190行 |

### 6. 注释

| 位置 | 当前文字 | 目标文字 | 说明 |
|------|----------|----------|------|
| HTML注释 | `<!-- 手机号输入 -->` | `<!-- 邮箱输入 -->` | 第173行 |

### 7. 变量名（可选，向后兼容）

| 变量 | 当前名 | 目标名 | 说明 |
|------|--------|--------|------|
| 表单字段 | `phone` | `email` | 第11行 |
| 错误变量 | `phoneError` | `emailError` | 第17行 |
| 验证函数 | `validatePhone` | `validateEmail` | 第21行 |

## 二、注册页面 (RegisterView.vue)

### 1. 表单标签文字

| 位置 | 当前文字 | 目标文字 | 说明 |
|------|----------|----------|------|
| label标签 | `手机号` | `邮箱` | 第225行 |

### 2. 输入框占位符

| 位置 | 当前文字 | 目标文字 | 说明 |
|------|----------|----------|------|
| placeholder | `请输入手机号（中国大陆）` | `请输入邮箱地址` | 第236行 |

### 3. 验证错误提示

| 场景 | 当前文字 | 目标文字 | 说明 |
|------|----------|----------|------|
| 空字段验证 | `请输入手机号` | `请输入邮箱地址` | 第39行 |
| 格式错误验证 | `请输入有效的手机号（中国大陆）` | `请输入有效的邮箱地址` | 第43行 |

### 4. HTML属性

| 属性 | 当前值 | 目标值 | 说明 |
|------|--------|--------|------|
| id | `phone` | `email` | 第231行 |
| autocomplete | `tel` | `email` | 第238行 |

### 5. 注释

| 位置 | 当前文字 | 目标文字 | 说明 |
|------|----------|----------|------|
| HTML注释 | `<!-- 手机号输入 -->` | `<!-- 邮箱输入 -->` | 第223行 |

### 6. 变量名（可选，向后兼容）

| 变量 | 当前名 | 目标名 | 说明 |
|------|--------|--------|------|
| 表单字段 | `phone` | `email` | 第12、21行 |
| 错误对象键 | `phone` | `email` | 第21行 |
| 验证函数 | `validatePhone` | `validateEmail` | 第36行 |

### 7. console.log输出（开发时使用）

| 场景 | 当前输出 | 目标输出 | 说明 |
|------|----------|----------|------|
| 注册参数输出 | `phone: formData.value.phone` | `email: formData.value.email` | 第97行 |
| 验证失败输出 | `phone: formErrors.value.phone` | `email: formErrors.value.email` | 第85行 |

## 三、其他可能需要变更的文件

### 1. 认证状态管理 (stores/auth.ts)

| 位置 | 当前文字 | 目标文字 | 说明 |
|------|----------|----------|------|
| 函数注释 | `手机号密码登录` | `邮箱密码登录` | 第103行 |
| 函数注释 | `手机号注册` | `邮箱注册` | 第208行 |
| 函数注释 | `手机号验证码登录` | `邮箱验证码登录` | 第168行 |

### 2. 其他组件

需要检查以下组件是否显示手机号相关信息：
- `src/components/common/UserMenu.vue` - 用户菜单
- `src/components/DebugPanel.vue` - 调试面板
- `src/views/MainView.vue` - 主页面

## 四、国际化考虑

如果项目未来需要国际化，需要考虑：

### 1. 准备多语言键值对

```javascript
// i18n keys
const keys = {
  login: {
    emailLabel: 'login.form.email.label',  // '邮箱'
    emailPlaceholder: 'login.form.email.placeholder',  // '请输入邮箱地址'
    emailRequired: 'login.form.email.required',  // '请输入邮箱地址'
    emailInvalid: 'login.form.email.invalid',  // '请输入有效的邮箱地址'
    loginFailed: 'login.form.loginFailed',  // '登录失败，请检查邮箱和密码'
  },
  register: {
    emailLabel: 'register.form.email.label',  // '邮箱'
    emailPlaceholder: 'register.form.email.placeholder',  // '请输入邮箱地址'
    emailRequired: 'register.form.email.required',  // '请输入邮箱地址'
    emailInvalid: 'register.form.email.invalid',  // '请输入有效的邮箱地址'
  }
}
```

## 五、变更验证清单

完成文字变更后，需要验证以下内容：

### 1. 登录页面验证
- [ ] 表单标签显示"邮箱"
- [ ] 占位符显示"请输入邮箱地址"
- [ ] 空字段错误提示："请输入邮箱地址"
- [ ] 格式错误提示："请输入有效的邮箱地址"
- [ ] 登录失败提示："登录失败，请检查邮箱和密码"

### 2. 注册页面验证
- [ ] 表单标签显示"邮箱"
- [ ] 占位符显示"请输入邮箱地址"
- [ ] 空字段错误提示："请输入邮箱地址"
- [ ] 格式错误提示："请输入有效的邮箱地址"

### 3. 其他验证
- [ ] HTML id属性正确
- [ ] autocomplete属性正确
- [ ] 变量名一致（如果修改了）
- [ ] console.log输出正确（如果修改了）

## 六、注意事项

1. **保持一致性**：确保所有相关文字都进行了更新
2. **用户友好性**：确保新的错误提示清晰易懂
3. **准确性**：邮箱验证提示应准确反映验证规则
4. **完整性**：不要遗漏任何显示"手机号"的地方

---

**创建时间**: 2026-01-11
**文档版本**: 1.0
